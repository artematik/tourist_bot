**Гуляй-нн** — это Telegram-бот, который создаёт персонализированные туристические маршруты на основе интересов пользователя, времени прогулки и выбранного способа передвижения.  
Бот использует данные с карт и искусственный интеллект для составления оптимальных прогулочных маршрутов.

## 🚀 Основная идея

Современные туристы тратят много времени на поиск достопримечательностей и составление маршрутов.  
Мы решили автоматизировать этот процесс: бот анализирует предпочтения пользователя и сам предлагает оптимальный маршрут — будь то прогулка пешком, на велосипеде или машине.

## ⚙️ Техническая реализация

1. **API карт** — используется для получения списка достопримечательностей и интересных мест поблизости (POI).
2. **AI-модель** — нейросеть принимает данные с карт, а также параметры пользователя:
   - интересы (например, музеи, парки, кафе);
   - время, когда он хочет начать прогулку;
   - длительность маршрута;
   - способ передвижения.
3. **Оптимизация маршрута** — AI анализирует точки и формирует логически связанный, удобный и интересный маршрут.
4. **Telegram-интерфейс** — через бот пользователь получает готовый маршрут и может сразу начать прогулку.

## 🖥 Серверная инфраструктура

Для обеспечения стабильной работы и доступности 24/7 бот развёрнут на **удалённом сервере (Linux)**.  
Это позволяет:

📍 **Текущая конфигурация:**
- **Сервер:** Ubuntu 22.04 LTS  
- **Расположение проекта:** `/home/user1/app/`  
- **Сервис:** `python-bot.service` (запускается через systemd)  
- **Python:** `/usr/bin/python3`  
- **Режим:** постоянный, автоматический перезапуск  

## 🥇 Почему наш сервис лучший

- **Персонализация** — каждый маршрут уникален, подстраивается под конкретного пользователя.
- **Скорость** — маршрут довольно быстро создается, без необходимости искать вручную.
- **Гибкость** — можно задать любые параметры: время, длительность, тип прогулки, настроение и т.д.
- **Построение в Яндекс и Google картах** - пользователю не нужно искать все выданные места в картах, это уже автоматически делается за него, ему только необходимо перейти по ссылке
- **Дальнейшие перспективы развитие** - c помощью такого подхода в дальнейшем можно оптимизировать маршруты (например ночью некоторые места будут закрыты и маршрут не будет затрагивать такие места)

## 🧩 Технологии

- Python 3.10+  
- Aiogram — Telegram API  
- httpx — запросы к внешним API  
- Pydantic / dotenv — хранение и валидация настроек  
- Ionet API / карты — источники данных о достопримечательностях  
- AI API — генерация и оптимизация маршрута  

## 🖥 Архитектура проекта

bot.py # Точка входа
config.py # Настройки, токены и API-ключи
handlers/ # Обработка сообщений и логика диалога
services/ # Работа с API и AI
utils/ # Вспомогательные функции
states.py # Машина состояний
requirements.txt # Зависимости