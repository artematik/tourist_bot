import logging
from typing import Optional, Tuple
from config import settings

class Geocoder:
    """Упрощенный геокодер - использует координаты по умолчанию"""
    
    @staticmethod
    async def get_coordinates(address: str) -> Tuple[float, float]:
        """
        Всегда возвращает координаты центра Нижнего Новгорода
        """
        logging.info(f"📍 Геокодер отключен. Используем центр Нижнего Новгорода для: {address}")
        return (56.326887, 44.005986)  # Центр Нижнего Новгорода
    
    @staticmethod
    async def get_address_from_coords(lat: float, lon: float) -> str:
        """
        Возвращает общее описание для координат
        """
        return "📍 Ваше местоположение в Нижнем Новгороде"